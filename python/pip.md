# pip

```
apt install python3-pip         # install

pip3 install --user <package>   # force local package installation
pip3 install -r requirement.txt # install from file

pip3 install --upgrade pip      # upgrade
pip3 uninstall <package>        # uninstall
pip3 freeze                     # Output installed packages in requirements format.
```
